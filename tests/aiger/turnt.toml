[envs.raw]
command = "../../target/debug/aig {filename} --stdout"
out_dir = "../outputs"
output."raw.dot" = "-"

[envs.opt]
command = "../../target/debug/aig {filename} --pre-optimize --stdout"
out_dir = "../outputs"
output."opt.dot" = "-"
