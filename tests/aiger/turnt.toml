[envs.raw]
command = "../../target/debug/aig dot {filename}"
out_dir = "../outputs"
output."raw.dot" = "-"

[envs.opt]
command = "../../target/debug/aig dot {filename} --pre-optimize"
out_dir = "../outputs"
output."opt.dot" = "-"
